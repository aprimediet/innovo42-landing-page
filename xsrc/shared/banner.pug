mixin main-banner-left-align(logo = null, title, subtitle, captions, image = null, includeButton = false, buttonUrl = null, buttonLabel = null)
  .w-100.banner-bg.banner-bg--left-align
  .d-flex.flex-row.w-100.h-100.px-10p.align-items-center
    .d-flex.flex-column.mr-auto
      h1 Hey 
    if image
      .d-none.d-xl-flex.flex-column.banner-images--container
        img.w-100.h-100(src=image, alt=title)